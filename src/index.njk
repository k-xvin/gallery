---
layout: base.njk
title: Kevin Xu | Generative Art
---

<div class="gallery">
    {% for image in images %}
    <div class="panel">
        <img src="/img/{{image}}" loading="lazy"/>
        <div class="title">{{image | replace(".jpg", "")}}</div>
    </div>
    {% endfor %}
</div>